#include <iostream>
#include <string>
using namespace std;
int main() {
    int numlen, sum = 0;
    string str;
    cin >> numlen;
    cin >> str;
    for (int i = 0; i < numlen; i++) {
        sum += int(str[i] - '0');
    }
    cout << sum;
}
